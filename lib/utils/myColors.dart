import 'package:flutter/material.dart';

const Color primaryColor = Color(0xffeb5757);
const Color primaryBgColor = Color(0xfffdf8f8);

const MaterialColor primarySwatchColor = const MaterialColor(
  0xffeb5757,
  const <int, Color>{
    50: primaryColor,
    100: primaryColor,
    200: primaryColor,
    300: primaryColor,
    400: primaryColor,
    500: primaryColor,
    600: primaryColor,
    700: primaryColor,
    800: primaryColor,
    900: primaryColor,
  },
);
